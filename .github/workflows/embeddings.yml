name: Wiki — Build, BM25 Index & Publish

on:
  push:
    paths:
      - 'docs/wiki/**'
      - 'mkdocs.yml'
      - '.github/workflows/embeddings.yml'
      - '.github/workflows/requirements.txt'
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      docs_changed: ${{ steps.docs_hash.outputs.skip }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      # libcairo2, libpango, curl are pre-installed on ubuntu-latest — no apt-get needed.
      # The social plugin downloads fonts via Google Fonts API and caches them in .cache/.

      - name: Restore social plugin cache (fonts + card manifest)
        uses: actions/cache@v4
        with:
          path: .cache/plugin/social
          key: ${{ runner.os }}-social-v2-${{ hashFiles('mkdocs.yml') }}
          restore-keys: |
            ${{ runner.os }}-social-v2-

      - name: Restore pip cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('.github/workflows/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Restore wheelhouse
        uses: actions/cache@v4
        with:
          path: ./.wheels
          key: ${{ runner.os }}-wheels-${{ hashFiles('.github/workflows/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-wheels-

      - name: Build wheels and install
        run: |
          python -m pip install --upgrade pip
          mkdir -p ./.wheels
          pip wheel --wheel-dir ./.wheels -r .github/workflows/requirements.txt || true
          pip wheel --wheel-dir ./.wheels mkdocs mkdocs-material[imaging] mkdocs-minify-plugin || true
          pip install --no-index --find-links ./.wheels -r .github/workflows/requirements.txt
          pip install --no-index --find-links ./.wheels mkdocs mkdocs-material[imaging] mkdocs-minify-plugin

      - name: Build MkDocs site
        run: mkdocs build

      - name: Check docs hash (skip if unchanged)
        id: docs_hash
        run: |
          CURRENT_HASH=${{ hashFiles('docs/wiki/**') }}
          OWNER=${{ github.repository_owner }}
          REPO=${{ github.event.repository.name }}
          PREV_HASH=$(curl -fsS "https://raw.githubusercontent.com/${OWNER}/${REPO}/gh-pages/wiki_index.hash" || true)
          if [ -n "$PREV_HASH" ] && [ "$CURRENT_HASH" = "$PREV_HASH" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Build BM25 index
        if: ${{ steps.docs_hash.outputs.skip == 'false' }}
        run: |
          python docs/wiki/build_bm25_index.py --docs-dir docs/wiki --output site/wiki_index.json --gzip
          echo "${{ hashFiles('docs/wiki/**') }}" > site/wiki_index.hash

      - name: Gzip index and size-check
        if: ${{ steps.docs_hash.outputs.skip == 'false' }}
        run: |
          if [ -f site/wiki_index.json.gz ]; then
            s=$(wc -c < site/wiki_index.json.gz)
            echo "gz size $s"
            if [ "$s" -gt $((500*1024*1024)) ]; then
              echo "INDEX TOO LARGE $s"
              exit 1
            fi
          else
            echo 'no index produced'
            exit 1
          fi

      - name: Publish site
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site


